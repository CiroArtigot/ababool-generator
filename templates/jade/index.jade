extends layout

block content

  #preloader(class="preloader", ng-hide="preloader")

  #pt-main(class="pt-perspective"  ng-init="current='" + cur + "'")

    input(
      type='hidden'
      name='current'
      value="{{current}}"
    )

    - var thepages = pages
        each value, index in thepages
          if !value.ajax
            div(
            id= index
            ng-class= "theclass('" + index + "')"
            data-left= value.left
            data-right= value.right
            data-down= value.down
            data-up= value.up
            animationend="endpage"
            class="pt-page"
            ng-show="isvisible('" + index + "')"
            )
              div(
              class="container clearfix"
              ng-show="viewcontainer('" + index + "')"
              )
                include pages/contents.jade

    #navigator(ng-show="navigator")
      div(class="navigator" ng-show="navigator")
        div(class="navigator_arrow arrow_right" ng-show="navright" ng-click="gotopage(right)")
          span(class="glyphicon glyphicon-chevron-right")
        div(class="navigator_arrow arrow_left" ng-show="navleft" ng-click="gotopage(left)")
          span(class="glyphicon glyphicon-chevron-left")
        div(class="navigator_arrow arrow_up" ng-show="navup" ng-click="gotopage(up)")
          span(class="glyphicon glyphicon-chevron-up")
        div(class="navigator_arrow arrow_down" ng-show="navdown" ng-click="gotopage(down)")
          span(class="glyphicon glyphicon-chevron-down")
