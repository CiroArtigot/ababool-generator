div(class="box")
  p(class="breadcrumb")
    | This post is published on <a href="/">Ababool</a> > <a href="/blog">Blog</a>

  h1 #{params[0].title}

  div(class="box4")
    each post in data
      div(class="separator")
        h3 !{post.title}
        p
          | !{post.teaser}
        div
          | <a href="#{conf.general.domain + 'amp/' + post.slug}" class="link">Read post</a>

  include ../../../../ababool_plugins/blog/blogfooter_amp.jade
