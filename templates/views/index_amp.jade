extends layout_amp

block content

  include amp/google-analytics.html

  - var thepages = pages
      each value, index in thepages
        if value.id == params[0].current
          include pages/amp/contents.jade
